name: recovery
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Set up secret file
      env:
        DEBUG_PASSWORD: ${{ secrets.GPG_PASSPHRASE }}
      run: |
        echo $DEBUG_PASSWORD >> /tmp/secrets.txt

    - name: Setup tmate session
      uses: mxschmitt/action-tmate@v3
